spring.application.name=mobile
# Server configuration
server.port=8080

# Logging
logging.level.root=INFO
logging.level.com.satadru.mobile=DEBUG

# JDBC
spring.datasource.url=jdbc:mysql://localhost:3306/sata_sih?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
spring.datasource.username=root
spring.datasource.password=Satadru345@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Prevent Spring Boot from auto-running schema/data SQL files
spring.sql.init.mode=never